test: add regression tests for 5 bug fixes

- TestHasBoilerplateNotConfusedByData: data string containing
  "section .text" no longer falsely skips standalone wrapper.
- TestCollectMemRefsNestedBrackets: nested brackets like
  [arr + [idx*4]] correctly extract both labels.
- TestDeadCodeElimInt: DCE keeps write-to-v0 before INT
  (readRegs INT fix prevents incorrect removal).
- TestConstPropagateInt: constant propagation does not fold
  ADD across INT that clobbers ABI registers.
- TestStrengthReduceMul3Op already covers the embedded-newline
  split (now returns 2 separate slice entries).

Co-Authored-By: AtomCode (deepseek-v4-flash) <noreply@atomgit.com>
